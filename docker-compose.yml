services:
  claudia:
    image: ${REGISTRY}/claudia:latest
    pull_policy: always
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    env_file: .env
    volumes:
      - ./data:/data

# volumes:
#   claudia-data:
